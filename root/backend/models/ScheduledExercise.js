const db = require("../config/db");
class ScheduledExercise
{
  constructor(id, sets, reps, rest, weight)
  {
    this.id = id;
    this.exercise = exercise;
    this.sets = sets;
    this.reps = reps;
    this.rest = rest;
    this.weight = weight;
  }
}
module.exports = ScheduledExercise